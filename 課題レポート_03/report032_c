%----------------------------------------------------------------------;
% Collinear: 共線 における各点の配置と与式の関係
%----------------------------------------------------------------------;

%%% A(a1, a2) B(b1, b2) C(c1, c2)

% A,B and C are collinear.
% procedure collinear(a1,a2,b1,b2,c1,c2)$
% begin
%         scalar a;
%         a:=(a1-b1)*(b2-c2)-(a2-b2)*(b1-c1);
%         return a
% end$
%----------------------------------------------------------------------;

% 与式f:=(a1-b1)*(b2-c2)-(a2-b2)*(b1-c1);
% ＜証明＞

%%% まず, 直線ABの一次関数の方程式を求める
%%% 次に, A-B上にCがあると仮定する
%%% 最後に, 結論として与式と同様の形の一次関数が求められる

%----------------------------------------------------------------------;
% グレブナー基底による検証

